// Formatted Input Function in C 

#include <stdio.h>

void main() {
    // int a, b, sum;
    // scanf("%d %d", &a, &b);

    // scanf("%4d %d", &a, &b);
    // sum = a + b;
    // printf("sum: %d", sum);

    char ch;
    scanf("%c", &ch);
    printf("character\n: %c", ch);

    return;

}